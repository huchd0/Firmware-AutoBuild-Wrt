name: ImmortalWrt-build-x86-64

on:
  workflow_dispatch:
    inputs:
      openwrt_version:
        description: '选择 ImmortalWrt 版本 (Auto 抓取最新稳定版/RC版)'
        required: false
        default: 'Auto'
        type: choice
        options:
          - 'Auto'
          - '24.10.5'
          - '25.12.0-rc1'
      custom_router_ip:
        description: '路由器管理地址 (LAN IP)'
        required: true
        default: '192.168.100.1'
        type: string
      profile:
        description: 'RootFS 分区大小 (MB)'
        required: true
        default: '1024'
        type: string

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE20: true 
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: 🔍 解析版本号
        id: resolve_version
        run: |
          if [ "${{ inputs.openwrt_version }}" == "Auto" ]; then
            echo ">>> 正在自动抓取 Docker Hub 上最新的 ImmortalWrt 标签..."
            LATEST_TAG=$(curl -sL "https://hub.docker.com/v2/repositories/immortalwrt/imagebuilder/tags?page_size=100&name=x86-64-openwrt-" | jq -r '.results[].name' | grep -v "SNAPSHOT" | grep -vE "^x86-64-openwrt-[0-9]+\.[0-9]+$" | head -n 1 | sed 's/x86-64-openwrt-//')
            
            if [ -z "$LATEST_TAG" ] || [ "$LATEST_TAG" == "null" ]; then
              echo "❌ 无法获取版本号，工作流中止。"
              exit 1
            fi
            echo "FINAL_VERSION=$LATEST_TAG" >> $GITHUB_ENV
          else
            echo "FINAL_VERSION=${{ inputs.openwrt_version }}" >> $GITHUB_ENV
          fi

      - name: Prepare Dirs and Scripts
        run: |
          mkdir -p ${{ github.workspace }}/bin
          mkdir -p ${{ github.workspace }}/files
          sed -i 's/\r$//' ${{ github.workspace }}/x86-64/ImmortalWrt-build-x86-64.sh
          chmod +x ${{ github.workspace }}/x86-64/ImmortalWrt-build-x86-64.sh

      - name: Run Docker ImageBuilder
        run: |
          docker run --rm -i \
            --user root \
            -v "${{ github.workspace }}/bin:/home/build/immortalwrt/bin" \
            -v "${{ github.workspace }}/files:/home/build/immortalwrt/files" \
            -v "${{ github.workspace }}/x86-64/ImmortalWrt-build-x86-64.sh:/home/build/immortalwrt/build.sh" \
            -e ROOTFS_SIZE="${{ inputs.profile }}" \
            -e MANAGEMENT_IP="${{ inputs.custom_router_ip }}" \
            immortalwrt/imagebuilder:x86-64-openwrt-${{ env.FINAL_VERSION }} \
            /bin/bash /home/build/immortalwrt/build.sh

      - name: Create Info Doc
        run: |
          cat > ${{ github.workspace }}/router-info.md <<EOF
          ## 🌟 ImmortalWrt ${{ env.FINAL_VERSION }} 轻量纯净定制版 (Wi-Fi 7 Ready)
          
          ### 💻 访问信息
          - 🏠 **管理后台**: http://${{ inputs.custom_router_ip }}
          - 👤 **默认账号**: root
          - 🔑 **默认密码**: (空)
          
          ### 💾 分区布局 (写入闪存需求)
          - 💿 **内核分区 (Kernel)**: 64 MB
          - 🗄️ **系统分区 (RootFS)**: ${{ inputs.profile }} MB
          
          ### 📶 Wi-Fi 7 无线网络 (已集成 MT7925 驱动)
          - 📡 **名称 (SSID)**: \`mywifi7\`
          - 🔒 **Wi-Fi密码**: \`Aa666666\`
          - ⚙️ **配置**: 5GHz / 149信道 / sae-mixed
          
          ### 🔌 核心特性
          - 🌐 **智能网口**: eth0 自动识别为 WAN，其余网口桥接为 LAN。
          - 🚀 **插件集成**: OpenClash (Meta核心), HomeProxy, DDNS-GO, Ksmbd。
          - 🛠️ **系统优化**: 自动挂载 sda3 大分区，支持无损扩容；内置全自动静默升级脚本。
          - 🎨 **界面**: Argon 极简主题。
          EOF

      - name: Upload Release
        uses: softprops/action-gh-release@v2.4.1
        with:
          tag_name: "${{ env.FINAL_VERSION }}-Build-${{ github.run_id }}"
          name: "🚀 ImmortalWrt ${{ env.FINAL_VERSION }} [IP: ${{ inputs.custom_router_ip }} | RootFS: ${{ inputs.profile }}MB]"
          body_path: "${{ github.workspace }}/router-info.md"
          files: |
            ${{ github.workspace }}/bin/targets/x86/64/*squashfs-combined-efi.img.gz
            ${{ github.workspace }}/bin/targets/x86/64/sha256sums
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
